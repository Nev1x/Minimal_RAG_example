# контейнер с Qdrant
services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: qdrant
    ports:
      - "6333:6333"    # HTTP API
      - "6334:6334"    # gRPC API
    volumes:
      - ./qdrant_storage:/qdrant/storage  # сохраняем данные на диск
    restart: unless-stopped